name: UMLgen-py310
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - plantuml
  - pylint       # Esto incluye pyreverse
  - graphviz     # Para el comando 'dot' que genera imágenes
